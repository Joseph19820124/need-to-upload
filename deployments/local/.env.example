# GitHub Configuration
# GitHub Personal Access Token
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo, read:user
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Optional: Override default host (default: 0.0.0.0)
# GITHUB_MCP_HOST=localhost

# Optional: Override default port (default: 8080)
# GITHUB_MCP_PORT=3000

# Optional: Enable read-only mode (default: false)
# GITHUB_MCP_GITHUB_READ_ONLY=true

# Server Configuration  
GITHUB_MCP_HOST=0.0.0.0
GITHUB_MCP_PORT=8080
GITHUB_MCP_GITHUB_READ_ONLY=false

# Optional: TLS Configuration
# GITHUB_MCP_TLS_CERT=/app/ssl/cert.pem
# GITHUB_MCP_TLS_KEY=/app/ssl/key.pem